spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-rrg-standalone:3306/rajratnaglobaldatabase?autoReconnect=true&useSSL=false
    username: rrg
    password: rrg123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://service-registry:8761/eureka
  instance:
    hostname: service-registry
